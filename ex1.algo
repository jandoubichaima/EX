ALGORITHM ex1
VAR
ph:STRING ;
n,i:INTEGER;
BEGIN
    write("ecrire une phrase :")
    read(ph)
    n:=0;
    for i:=1 to len(ph)
       if (Mid(ph,i,1)= " " )
       n:=n+1 ;
      write( "Cette phrase compte ", n + 1, " mots")
END
